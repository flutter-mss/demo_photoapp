name: demo_photo_gallery
description: Grid gallery with color sample images
version: 1.0.0

environment:
  sdk: ^3.0.0
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  mss_core:
    path: ../mss_core
  demo_gallery_module_def:
    path: ../demo_gallery_module_def
